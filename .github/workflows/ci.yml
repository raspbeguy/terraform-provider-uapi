name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: build & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - run: go build ./...
      - run: go vet ./...
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi
      - run: go test -race ./...

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.5.0
      - run: $(go env GOPATH)/bin/golangci-lint run ./...

  generate:
    name: generated code & docs up to date
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      # go generate runs the OpenAPI code generator first, then tfplugindocs.
      - run: go generate ./...
      - name: check generated resources, examples, and docs are committed
        run: |
          if [ -n "$(git status --porcelain internal/provider examples docs)" ]; then
            echo "Generated output is out of date. Run 'make docs' and commit the result."
            git status --porcelain internal/provider examples docs
            git diff -- internal/provider examples docs
            exit 1
          fi

  acceptance:
    name: acceptance (mock)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false
      - name: acceptance tests against the in-process fake uapi
        run: TF_ACC=1 go test ./internal/provider/ -run TestAcc -count=1 -timeout 15m
